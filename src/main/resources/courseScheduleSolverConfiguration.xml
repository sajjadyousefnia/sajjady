<solver>
    <solutionClass>calculations.CourseSchedule</solutionClass>
    <entityClass>calculations.Lecture</entityClass>
    <!-- <solutionClass>calculations.CourseSchedule</solutionClass>
     <entityClass>calculations.Lecture</entityClass>-->
    <scoreDirectorFactory>
        <incrementalScoreCalculatorClass>calculations.IncrementalScoreCalculator</incrementalScoreCalculatorClass>
    </scoreDirectorFactory>
    <termination>
        <secondsSpentLimit>10</secondsSpentLimit>
    </termination>
   <!-- <termination>
        <bestScoreLimit>600hard/0soft</bestScoreLimit>
    </termination>-->
</solver>